# Groksandbox

Exploration sandbox.

## ORL opportunity health

The clickable v0 scaffold is in [`orl-opportunity-health/`](orl-opportunity-health/).

```bash
cd orl-opportunity-health
npm install
npm run dev
```

Open http://localhost:5173. That folder’s README explains the three views: weekly coverage hashes, monthly squares, and the health ring.
