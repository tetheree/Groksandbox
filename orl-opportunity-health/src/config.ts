/**
 * Parked from the Sep 2 notes: a this-week delta strip
 * (Todd’s 24h add-on treated as two 12h units, Friday ingest → Monday viewer).
 * Flip to true to show the placeholder. Not part of the v0 story.
 */
export const SHOW_DELTA_STRIP: boolean = false
